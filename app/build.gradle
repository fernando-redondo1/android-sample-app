// ... dentro de defaultConfig
externalNativeBuild {
    ndkBuild {
        // CAMBIO: Añadir el signo '='
        path = "src/main/jni/Android.mk" 
    }
}

// ... y al final del bloque android
externalNativeBuild {
    ndkBuild {
        // CAMBIO: Añadir el signo '='
        path = "src/main/jni/Android.mk"
    }
}
