android {
    // ... resto de tu config

    defaultConfig {
        // ... otras opciones
        
        externalNativeBuild {
            ndkBuild {
                // Asegúrate de que no haya espacios extra antes del '='
                path = file("src/main/jni/Android.mk") 
            }
        }
    }
    
    // IMPORTANTE: Borra cualquier otro bloque externalNativeBuild que esté fuera de defaultConfig
}
