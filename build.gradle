buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        // Plugin actualizado para estabilidad total
        classpath 'com.android.tools.build:gradle:8.2.2'
    }
}

allprojects {
    repositories {
        google()
        mavenCentral()
    }
}

task clean(type: Delete) {
    delete rootProject.buildDir
}
